import styles from "../../styles/footer.module.css"

const Footer = () => {
  return (
    <div className={styles.footer}>
      <h2 className={styles.title}>&copy; NEXT JS MOVIE APP</h2>
    </div>
  )
};

export default Footer;